PREFIX odrl: <http://www.w3.org/ns/odrl/2/>
PREFIX dcterms: <http://purl.org/dc/terms/>
PREFIX tosl: <https://w3id.org/tosl/>

SELECT (COUNT(DISTINCT ?permission) AS ?totalPermissions)
WHERE {
  ?permission a odrl:Permission .
}